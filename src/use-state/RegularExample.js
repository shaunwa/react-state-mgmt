import { CounterButton } from './CounterButton';

export const RegularExample = () => {
    return (
        <>
        <h2>This is the regular example:</h2>
        <CounterButton />
        </>
    );
}